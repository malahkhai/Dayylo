---
description: Auto-commit and push major changes
---

// turbo-all
1. Add all changes to git
   `git add .`
2. Commit with a descriptive message
   `git commit -m "Major update: Implement Insights, Profile screens and Reanimated transitions"`
3. Push to remote
   `git push origin main`
